<h1><%= @artist.name %></h1>
<h2><%= @artist.song_count %> song</h2>
<ul>
  <li>
    <% @artist.songs.each do |song| %>
      <h2><%= link_to song.title, song_path(song.id) %></h2>
    <% end %>
  </li>
</ul>
