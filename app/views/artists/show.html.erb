<h1><%= @artist.name %></h1>

 
#  <h1><%= @artist.song_count %> songs</h1> 
<ul>
  <% @artist.songs.each do |a| %>
    <li><%= link_to a.title, song_path(a) %><%= @artist.song_count %></li>
  <% end %>
</ul>